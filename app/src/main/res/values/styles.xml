<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <!-- Layouts -->
    <style name="RootLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/mainBackground</item>
    </style>

    <style name="RootLayout.Cell">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:paddingTop">@dimen/spacing_medium</item>
        <item name="android:paddingBottom">@dimen/spacing_medium</item>
        <item name="android:paddingStart">@dimen/spacing_medium</item>
        <item name="android:paddingEnd">@dimen/spacing_medium</item>
    </style>

    <style name="CardView.Cell">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">@dimen/spacing_medium</item>
        <item name="android:layout_marginEnd">@dimen/spacing_medium</item>
        <item name="android:layout_marginTop">@dimen/spacing_medium</item>
    </style>

    <!-- Text Views -->
    <style name="TextViewDefault">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="TextBlack" parent="TextViewDefault">
        <item name="android:textColor">@color/textBlack</item>
    </style>

    <style name="TextBlack.SectionHeader" >
        <item name="android:textSize">@dimen/text_size_section_header</item>
    </style>

    <style name="TextBlack.Paragraph" >
        <item name="android:textSize">@dimen/text_size_paragraph</item>
    </style>

    <style name="TextNeutral" parent="TextViewDefault">
        <item name="android:textColor">@color/textNeutral</item>
    </style>

    <style name="TextNeutral.ToolbarTitle" >
        <item name="android:textSize">@dimen/text_size_collapsing_toolbar_title</item>
    </style>

    <style name="TextNeutral.SectionHeader" >
        <item name="android:textSize">@dimen/text_size_section_header</item>
    </style>

    <!-- Lists -->
    <style name="RecyclerDefault">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="layoutManager">androidx.recyclerview.widget.LinearLayoutManager</item>
    </style>

    <!-- Images -->
    <style name="ImageDefault">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Loading -->
    <style name="DefaultOverlay">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/loadingOverlay</item>
        <item name="android:visibility">gone</item>
    </style>
</resources>